# BPPS base instances

This folder contains the original BPPS benchmark instances used as the base dataset.

These instances are extended by adding conflict graphs to generate the BPPSC benchmark set.

## Note

The conflict-free case `rho = 0.0` corresponds to the original BPPS benchmark instances.
