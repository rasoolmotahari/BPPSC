# Toy example

This folder contains a small BPPSC instance used only for testing the code.

The toy example is useful for checking:

- instance reading,
- feasibility checking,
- MSIG execution,
- lower-bound execution,
- benchmark heuristic execution.
