# Lower bound

The lower-bound procedure combines:

1. setup-capacity requirements,
2. lower bound on the number of required bins,
3. global maximal clique information,
4. class-specific maximal clique information.

The improved lower bound is designed to reflect both setup requirements and conflict restrictions.
